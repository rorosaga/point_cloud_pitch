// Pitch dimensions (scaled FIFA proportions)
export const PITCH_LENGTH = 10.5; // x-axis (meters) — forward direction
export const PITCH_WIDTH = 6.8;   // z-axis (meters) — lateral direction

// Reveal map resolution
export const REVEAL_TEX_SIZE = 256;

// Splat radius in texture pixels for each LiDAR point
export const SPLAT_RADIUS = 4;

// ROS WebSocket
export const ROSBRIDGE_URL = 'ws://localhost:9090';
export const POINTCLOUD_TOPIC = '/utlidar/cloud';
